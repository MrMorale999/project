# Frontend Angular

Applicazione Angular che consuma il microservizio Java backend.

## Installazione

1. Apri la cartella `frontend`
2. Esegui `npm install`

## Avvio

- `npm start`

Il frontend verrà eseguito su `http://localhost:4200` e il proxy reindirizzerà le chiamate API a `http://localhost:8080`.
